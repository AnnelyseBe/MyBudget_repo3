#sql database

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/mybudget_prod
    username: mybudget_prod_user
    password: Prod123456!?
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
#    onze productie moeten we niet vervuilen met sql statements
    show-sql: false
